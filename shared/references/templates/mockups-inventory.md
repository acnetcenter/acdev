# Mockups inventory

Skeleton of post-MVP screens. This is not a set of mockups — it is a list
that reserves each screen's name, purpose, and place in the navigation so
the MVP can be built ready to grow into them. Post-MVP screens are mocked up
just-in-time during `build`, phase by phase, through the `mockups` skill.

| Screen | Purpose | Phase | Navigation placement |
| --- | --- | --- | --- |
| <!-- e.g. Team billing | Manage seats and invoices for a team account | Phase 2 | Settings menu, new "Billing" item --> |

<!-- One row per post-MVP screen. Screen: short name. Purpose: one line.
     Phase: the target phase from VISION §7's phase sketch. Navigation
     placement: where it will attach once built (menu, sidebar section,
     settings tab, etc.). -->

## Navigation notes

<!-- How the MVP navigation visibly accommodates these future modules:
     disabled menu items, labeled sections reserved for later, placeholder
     slots in a sidebar. Reference the specific MVP mockup screen(s) where
     this is shown. No placeholders left silently unexplained. -->
