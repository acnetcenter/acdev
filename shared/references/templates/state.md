# acdev state

stage: <pipeline stage, e.g. build>
updated: <YYYY-MM-DD HH:MM>
acdev_version: <version of the acdev plugin that wrote this state>
language: <the project's documentation language, set via --lang and preserved across writes>
latest_checkpoint: <path to the newest file in .acdev/checkpoints/, relative to repo root>
