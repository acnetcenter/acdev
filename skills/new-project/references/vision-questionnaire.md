# Questionnaire for VISION.md (fixed 7-section structure)

VISION.md is the source of truth: every other document, and later the code
itself, derives from it. It is NOT autogenerated — it is conversed. It must
stay SIMPLE: one or two pages, readable in one sitting, with exactly these 7
sections.

## Procedure

1. Present the sections one at a time (blocks of 2-3 only if the user asks
   for speed). For every question, offer your best draft answer as a
   starting point, seeded from what the user has already told — they
   correct a draft better than they compose one cold.
2. Close each section by showing it AS DOCUMENT TEXT — "this is what we
   have so far" — and let the user correct or extend it right there before
   moving on. The preview is the document being written, not a chat
   summary; by the last section the user has seen every line of the
   future VISION.md.
3. Challenge weak answers: if the user says "for everyone," ask who is NOT a
   customer; if they say "as complete as possible," ask what is explicitly OUT
   of the v1. A good VISION says as much through what it excludes as through
   what it includes.
4. Only once all 7 sections are agreed — and seen — write `docs/VISION.md`.

## The 7 sections

### 1. What it is / what it is NOT

- What is the product, in ONE sentence, without jargon?
- What is it NOT / what is explicitly out of scope? This is just as important
  as the first answer — it is the vaccine against scope creep for every future
  AI session working on this project.

### 2. Model

Two distinct axes (they do not always move together):

- **Delivery / tenancy**: public multi-tenant SaaS · single-tenant for one
  client · on-premise · local/personal app.
- **Business**: product for sale · internal tool built for one company · personal use.

This is the highest-leverage section in the whole document — it decides
whether entire subsystems exist at all (tenant isolation, roles and
permissions, billing and plans, a support panel...). The answers here activate
golden router rules later at the blueprint stage (multi-tenant implies
verified isolation; sale implies signed payments/webhooks; and so on).

### 3. Who it is for

- Who is the target user, and what is their technical level?
- If the payer is not the user, who is each one?

### 4. Problem it solves

- What concrete problem does it solve, and how do users solve it TODAY,
  without this product?

### 5. What it does: features and capabilities

- A clear list of the system's features and capabilities, with the v1 items
  marked.
- The 3-5 critical end-to-end flows that absolutely must work, each one ending
  in an observable result (e.g., "signs up -> connects X -> obtains Y"), not
  in a screen.

### 6. Approach (how, at a high level)

- The product strategy in 2-4 sentences ("through AI agents that...", "with a
  pipeline X -> Y -> Z"...).
- WARNING: no architecture or stack talk here (database, frameworks,
  components) — that belongs to the derived ARCHITECTURE.md, produced at
  blueprint. If the conversation drifts into technical territory, capture the
  decision as blueprint input and steer the section back to product level.

### 7. Full-product phase sketch

- A one-line sketch of the phases that take the product from MVP to its
  complete vision, one line per phase. This is a sketch only — MVP detail
  itself now lives in its own document, `MVP.md`, produced at stage 2. Each
  post-MVP phase is designed in full detail just before it is built.

## Complementary questions (NOT part of VISION; feed the blueprint stage)

Ask these at the end of the conversation, making clear where the answers go:

- Preferred or recommended stack, hosting -> first ADRs and ARCHITECTURE.md.
- External integrations (APIs, AI, payments, email) and their third-party
  lead times / approval procedures -> INTEGRATIONS.md and ROADMAP.md (lead
  times can block phases).
- Jurisdictions and compliance: in which countries or regions will the
  product operate and hold user data, and which regulatory regimes does
  that activate (GDPR and its local kin, sector rules)? Include
  data-residency constraints -> the compliance ADR written at blueprint;
  operational detail lands in SECURITY.md. This is a user-challenge
  decision: asked, never assumed from the domain.
- Legal/privacy constraints (PII, GDPR...) and security constraints (roles,
  who sees what) -> SECURITY.md. These only rise into VISION if they are part
  of the product's identity.
- Decisions still open, and who owns deciding them -> a list in ROADMAP.md or
  a "pending" ADR with an owner.

## Final document structure

`docs/VISION.md` is written with these 7 sections, in the project's
documentation language, with this footer rule verbatim: "Inconsistencies
between documents and code are corrected in the documents or via ADR; this
document only changes by explicit decision of the product owner."
