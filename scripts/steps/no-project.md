No `.acdev/` in this project (or an unknown stage). Do not infer a stage from the code.

Offer the two user-run entry points and stop: `/acdev:onboard` if the repo already has code, `/acdev:new-project` if it is empty. Never invoke either yourself, not even to save a step.
