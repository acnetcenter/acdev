Stage intake: the project was opened by `new-project` (user-run) and no document is approved yet.

Continue the interview in the current conversation per the `new-project` skill: lead with the user's idea, then the VISION sections one at a time per the Procedure in `<plugin-root>/skills/new-project/references/vision-questionnaire.md` (scaffold `docs/VISION.md` with `node "<plugin-root>/scripts/acdev.mjs" scaffold vision docs/VISION.md`, replacing each `<!-- ... -->` guidance comment and each `<...>` placeholder with real content; draft each section first, challenge weak answers, show it as document text and Edit it in before moving on; no whole-document write). The gate asks "Do you approve this VISION document?" on the full document; on an explicit yes, `node "<plugin-root>/scripts/acdev.mjs" checkpoint write --stage mvp` and commit `docs: project vision`. Product code is denied by the guard until build. If this session has no `new-project` context, ask the user to run `/acdev:new-project` to resume; do not invoke it yourself.
