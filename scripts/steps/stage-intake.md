Stage intake: the project was opened by `new-project` (user-run) and no document is approved yet.

Continue the interview in the current conversation per the `new-project` skill: lead with the user's idea, then the VISION sections one at a time, each shown as document text for on-the-spot correction. Product code is denied by the guard until build. If this session has no `new-project` context, ask the user to run `/acdev:new-project` to resume; do not invoke it yourself.
