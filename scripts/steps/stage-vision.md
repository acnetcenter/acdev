Stage vision: `docs/VISION.md` is approved; the MVP is not cut yet (`new-project`, user-run).

Continue per the `new-project` skill: cut phase 1 from VISION into `docs/MVP.md` per the Procedure in `<plugin-root>/skills/new-project/references/mvp-guide.md` (`node "<plugin-root>/scripts/acdev.mjs" scaffold mvp docs/MVP.md`; IN list traceable to VISION §5 flows, OUT list naming each item's phase from §7, success criteria as verifiable facts), paste each section's full text in the reply before asking about it (an Edit diff is not a preview), and gate on the FULL document, pasted, with an explicit approval. A gap or contradiction found in VISION is fixed there first, with the user, never patched into MVP.md. The guard asks before VISION is edited again: a change there is a user-challenge decision. On approval, `node "<plugin-root>/scripts/acdev.mjs" checkpoint write --stage mockups` (or `--stage blueprint` when the project has no UI and the user confirms the skip) and commit `docs: mvp contract`. If this session has no `new-project` context, ask the user to run `/acdev:new-project`; do not invoke it yourself.
