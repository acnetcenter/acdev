Stage vision: `docs/VISION.md` is approved; the MVP is not cut yet (`new-project`, user-run).

Continue per the `new-project` skill: cut phase 1 from VISION into `docs/MVP.md` (references/mvp-guide.md), show each section as document text, and gate on the FULL document with an explicit approval. The guard asks before VISION is edited again: a change there is a user-challenge decision. If this session has no `new-project` context, ask the user to run `/acdev:new-project`; do not invoke it yourself.
