Stage mvp: `docs/MVP.md` is approved; the next gate is the mockups.

Run the `mockups` skill: one HTML page per MVP screen with realistic data (per `<plugin-root>/skills/mockups/references/mockups-guide.md`), empty/error/loading states for key screens as copies edited in `<main>`, `index.html` grouped by flow, every critical flow walkable, and `docs/mockups-inventory.md` (`node "<plugin-root>/scripts/acdev.mjs" scaffold mockups-inventory docs/mockups-inventory.md`) with one line per post-MVP screen and its place reserved in the MVP navigation. Before the gate, `node "<plugin-root>/scripts/acdev.mjs" mockup-spec --write` and complete one Intent line per screen in `mockups/SPEC.md`. The gate asks "Do you approve these mockups?" and only an explicit yes freezes them.
